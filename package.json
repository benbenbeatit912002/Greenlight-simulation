{
  "name": "greenlight-simulation-ui",
  "private": true,
  "scripts": {
    "format": "prettier --write frontend tests/*.js index.html styles.css",
    "format:check": "prettier --check frontend tests/*.js index.html styles.css",
    "test": "node --test tests/*.test.js"
  },
  "devDependencies": {
    "prettier": "3.8.1"
  }
}
